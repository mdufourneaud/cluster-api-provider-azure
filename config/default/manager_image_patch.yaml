apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: docker.io/mdufourneaud4datagalaxy/cluster-api-azure-controller-amd64:v1.6.1-alpha.4
        name: manager
